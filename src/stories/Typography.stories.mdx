import { Meta, Typeset } from '@storybook/addon-docs/blocks'
import { defaultTheme } from '../style/themes'

<Meta title="Design System/Typography" />

# Typography

**Font:** Roboto Slab

**Weights:** 300(thin), 400(regular), 600(bold)

<Typeset
  fontFamily="Roboto Slab"
  fontSizes={defaultTheme.fontSizes}
  fontWeight={defaultTheme.fontWeights.thin}
  sampleText="Typography sample with thin font weight"
/>

<Typeset
  fontFamily="Roboto Slab"
  fontSizes={defaultTheme.fontSizes}
  fontWeight={defaultTheme.fontWeights.regular}
  sampleText="Typography sample with regular font weight"
/>

<Typeset
  fontFamily="Roboto Slab"
  fontSizes={defaultTheme.fontSizes}
  fontWeight={defaultTheme.fontWeights.bold}
  sampleText="Typography sample with bold font weight"
/>
